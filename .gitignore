# Byte-compiled / optimized / DLL files
# These files are generated by Python and can be safely ignored.
*.pyc
*.pyo
*.pyd
__pycache__/
*.so

# C extensions
# C source files and headers that are not needed in version control.
*.c
*.cpp
*.h

# Distribution / packaging
# Files related to packaging and distribution that should not be versioned.
*.egg
*.egg-info/
*.dist-info/
*.wheel/
*.build/
*.pydevproject

# Virtual environment
# Ignore virtual environment directories to prevent including environment-specific files.
venv/
.env/
.venv/
ENV/
env/
*.virtualenv/

# If you use pipenv:
# Pipenv lock files to manage dependencies; typically not versioned in the repo.
Pipfile.lock

# IDEs and editors
# Editor and IDE-specific files that should not be included in version control.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo

# macOS Finder
# macOS Finder metadata files that are not needed in the repo.
.DS_Store

# Windows System files
# Windows-specific system files that should be ignored.
Thumbs.db
Desktop.ini

# Logs
# Log files which are typically generated at runtime and should not be versioned.
*.log
*.log.*

# Ignore the 'misc/' directory
misc/

# Ignore the 'scripts/' directory
scripts/

# Python environment management files
*.env
Pipfile
Pipfile.lock

# Ignore content in resources/images/
resources/images/*
# Do no ignore the .ignore file itself
!resources/images/.gitignore